# Preview.10 superseded

`v0.1.0-preview.10` was never publicly promoted. Its exact archive acceptance
failed because the Provider rejected the documented `--offer-id` flag. The
immutable Preview.10 tags, private prerelease, and unpromoted public draft
remain historical records;
use the gated [Preview.11](PREVIEW11.md) release source instead.
